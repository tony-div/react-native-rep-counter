///
/// HybridRepCounterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `RepCounterConfig` to properly resolve imports.
namespace margelo::nitro::repcounter { struct RepCounterConfig; }
// Forward declaration of `RepPhase` to properly resolve imports.
namespace margelo::nitro::repcounter { enum class RepPhase; }
// Forward declaration of `RepCounterState` to properly resolve imports.
namespace margelo::nitro::repcounter { struct RepCounterState; }

#include "RepCounterConfig.hpp"
#include <optional>
#include <vector>
#include <string>
#include "RepPhase.hpp"
#include "RepCounterState.hpp"

namespace margelo::nitro::repcounter {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `RepCounter`
   * Inherit this class to create instances of `HybridRepCounterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridRepCounter: public HybridRepCounterSpec {
   * public:
   *   HybridRepCounter(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridRepCounterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridRepCounterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridRepCounterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void startSession(const std::optional<RepCounterConfig>& config) = 0;
      virtual void stopSession() = 0;
      virtual void ingestLandmarksBuffer(const std::vector<double>& landmarks) = 0;
      virtual void ingestLandmarksBufferWithExercise(const std::vector<double>& landmarks, const std::string& exercise) = 0;
      virtual double getRepCount() = 0;
      virtual RepPhase getCurrentPhase() = 0;
      virtual RepCounterState getState() = 0;
      virtual void resetReps() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "RepCounter";
  };

} // namespace margelo::nitro::repcounter
