///
/// RepPhase.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::repcounter {

  /**
   * An enum which can be represented as a JavaScript union (RepPhase).
   */
  enum class RepPhase {
    UNKNOWN      SWIFT_NAME(unknown) = 0,
    UP      SWIFT_NAME(up) = 1,
    DOWN      SWIFT_NAME(down) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::repcounter

namespace margelo::nitro {

  // C++ RepPhase <> JS RepPhase (union)
  template <>
  struct JSIConverter<margelo::nitro::repcounter::RepPhase> final {
    static inline margelo::nitro::repcounter::RepPhase fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("UNKNOWN"): return margelo::nitro::repcounter::RepPhase::UNKNOWN;
        case hashString("UP"): return margelo::nitro::repcounter::RepPhase::UP;
        case hashString("DOWN"): return margelo::nitro::repcounter::RepPhase::DOWN;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum RepPhase - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::repcounter::RepPhase arg) {
      switch (arg) {
        case margelo::nitro::repcounter::RepPhase::UNKNOWN: return JSIConverter<std::string>::toJSI(runtime, "UNKNOWN");
        case margelo::nitro::repcounter::RepPhase::UP: return JSIConverter<std::string>::toJSI(runtime, "UP");
        case margelo::nitro::repcounter::RepPhase::DOWN: return JSIConverter<std::string>::toJSI(runtime, "DOWN");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert RepPhase to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("UNKNOWN"):
        case hashString("UP"):
        case hashString("DOWN"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
