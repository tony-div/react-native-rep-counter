///
/// HybridHybridRepCounterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `RepCounterConfig` to properly resolve imports.
namespace margelo::nitro::repcounter { struct RepCounterConfig; }
// Forward declaration of `RepCounterState` to properly resolve imports.
namespace margelo::nitro::repcounter { struct RepCounterState; }

#include <NitroModules/Null.hpp>
#include "RepCounterConfig.hpp"
#include <variant>
#include <optional>
#include <string>
#include "RepCounterState.hpp"
#include <vector>

namespace margelo::nitro::repcounter {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `HybridRepCounter`
   * Inherit this class to create instances of `HybridHybridRepCounterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridHybridRepCounter: public HybridHybridRepCounterSpec {
   * public:
   *   HybridHybridRepCounter(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridHybridRepCounterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridHybridRepCounterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridHybridRepCounterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void startSession(const std::optional<std::variant<nitro::NullType, RepCounterConfig>>& config) = 0;
      virtual void stopSession() = 0;
      virtual void setExercise(const std::optional<std::variant<nitro::NullType, std::string>>& exercise) = 0;
      virtual void resetReps() = 0;
      virtual void resetAll() = 0;
      virtual RepCounterState getState() = 0;
      virtual RepCounterState update(const std::vector<double>& landmarks, const std::optional<std::variant<nitro::NullType, std::string>>& exerciseOverride) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "HybridRepCounter";
  };

} // namespace margelo::nitro::repcounter
